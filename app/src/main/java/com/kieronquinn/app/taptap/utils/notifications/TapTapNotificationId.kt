package com.kieronquinn.app.taptap.utils.notifications

enum class TapTapNotificationId {
    //0 cannot be used for background notification
    UNUSED,
    BACKGROUND,
    SERVICE,
    ACTION,
    GATE,
    UPDATE,
    CRASH
}